services:
  icarus-calculator:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  icarus-refresh:
    build: .
    command: ["python", "-m", "app.scripts.refresh_loop"]
    volumes:
      - ./data:/app/data
    restart: unless-stopped
